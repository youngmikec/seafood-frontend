export enum UserType {
    SENDER = "SENDER",
    DISPATCHER = "DISPATCHER",
    ADMIN = "ADMIN", 
}

export enum Gender {
    MALE = "SENDER",
    FEMALE = "FEMALE",
    OTHER = "OTHER", 
}

export const PACKAGE = {
    STATUS: {
        PENDING: "PENDING",
        CHECKEDOUT: "CHECKEDOUT",
        PICKUP: "PICKUP",
        CANCELLED: "CANCELLED"
    }
}